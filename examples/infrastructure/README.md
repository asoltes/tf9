# Example Infrastructure Repo

This is a minimal repository layout that `tfops` can target during local
development.

It contains two Terraform target directories:

- `environments/dev`
- `environments/prod`

Both targets are intentionally no-op examples.
